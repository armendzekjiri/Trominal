import { EmptyPlaceholder } from './EmptyPlaceholder'

export function TunnelsPlaceholder() {
  return (
    <EmptyPlaceholder
      title="Tunnels"
      body="Local, remote, and SOCKS tunnel manager lands in Phase 6."
    />
  )
}
