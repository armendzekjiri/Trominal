//! `trominal-helper` — a slim local-loopback SSH/SFTP bridge for the web
//! build of Trominal. Phase 10 (M1.0) lands the workspace skeleton; the
//! WebSocket server, pairing handshake, and session bridging arrive in
//! subsequent commits on the same branch.

fn main() {
    println!(
        "trominal-helper {} (Phase 10 stub)",
        env!("CARGO_PKG_VERSION")
    );
}
